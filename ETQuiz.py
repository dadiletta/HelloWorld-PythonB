import random
students = []
def askStudents():
    i = 0
    con = "something"
    for con:
        con = input()
        students[i] = input("Enter a student name")
        i += 1

def makePairs():
    for i in students:

askStudents()
makePairs()
